version: 2

models:
  - name: stg_students
    description: "Staging : dédoublonnage + nettoyage léger."
    columns:
      - name: USER_ID
        tests:
          - not_null
          - unique
