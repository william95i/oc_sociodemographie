version: 2

models:
  - name: int_students
    description: "Modèle intermédiaire nettoyé et normalisé."
    columns:
      - name: gender
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["HOMME", "FEMME", "INCONNU"]
